import 'package:flutter/material.dart';

class InfoInputPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Enter Your Info")),
      body: Center(child: Text("User Info Input Screen")),
    );
  }
}

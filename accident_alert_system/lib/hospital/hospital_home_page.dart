import 'package:flutter/material.dart';

class HospitalHomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Enter Your Info")),
      body: Center(child: Text("hospital home  Screen")),
    );
  }
}
